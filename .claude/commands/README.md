# Codelens Project — Skills Index

Skills available for this project (invoke with `/skill-name`):

| Skill | File | Purpose |
|---|---|---|
| `/ai-review-bot` | `ai-review-bot.md` | Full guide for the AI Code Review Bot project |

## How to add more skills

Drop a `.md` file into `.claude/commands/` and it becomes a slash command.
Use `$ARGUMENTS` inside the file to capture text typed after the command name.
